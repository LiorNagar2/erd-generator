const SequelizeParser = require("./SequelizeParser");const MongooseParser = require("./MongooseParser");class ParserFactory {    static getParser(type, config) {        switch (type) {            case "sequelize":                return new SequelizeParser(config);            case "mongoose":                return new MongooseParser(config);            default:                throw new Error(`Unsupported type: ${type}`);        }    }}module.exports = ParserFactory;